<div data-controller="room_message">
  <%= form_with(model: @room, remote: true, class: "d-flex", id:"roomform" ) do |f| %>
    <%= f.text_field :name, class: "form-control", autocomplete: 'off' %>
    <%= f.submit data: { disable_with: false },id: "roomSubmitBtn" %>
  <% end %>
</div>
